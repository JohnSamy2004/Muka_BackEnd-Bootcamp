const arr = [1, 2, 3, 4, 5];

let add = arr.push(2); //add a value '2' to after last index in array and return the length

console.log(add);
console.log(arr);

let del = arr.pop(); //delete a value '2' from last index in array and return the deleted value

console.log(del);
console.log(arr);